# Скопируй значения в Coolify → Environment Variables
# (файл сам по себе не монтируется — переменные задаются в UI Coolify)

# ОБЯЗАТЕЛЬНО: публичный IPv4 сервера Coolify
SFU_ANNOUNCED_IP=203.0.113.10

# Публичный URL (после назначения домена в Coolify)
# Если Coolify magic URL пустой — задай вручную:
SERVER_HOST_URL=https://meet.example.com

# Уже есть дефолты в docker-compose.yaml:
# UI_LANGUAGE=ru
# APP_NAME=MiroTalk SFU
# SFU_SERVER=true
# SFU_MIN_PORT=40000
# SFU_MAX_PORT=40100
# HOST_PROTECTED=false
# RECORDING_ENABLED=false

# Пример защиты хоста:
# HOST_PROTECTED=true
# HOST_USER_AUTH=true
# HOST_USERS=admin:СмениПароль:Админ:*
